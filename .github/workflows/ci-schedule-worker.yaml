name: CI Schedule Worker

on:
  pull_request:
    branches: [main]
    paths:
      - 'schedule/handlers/**'
      - 'schedule/shared/**'
      - 'infrastructure/**'
      - 'shared/**'
      - '.github/workflows/cd-schedule-worker.yaml'
      - '.github/workflows/ci-schedule-worker.yaml'
      - 'deploy/docker-compose.schedule.worker.prod.yaml'
  push:
    branches: [main]
    paths:
      - 'schedule/handlers/**'
      - 'schedule/shared/**'
      - 'infrastructure/**'
      - 'shared/**'
      - '.github/workflows/cd-schedule-worker.yaml'
      - '.github/workflows/ci-schedule-worker.yaml'
      - 'deploy/docker-compose.schedule.worker.prod.yaml'
  workflow_dispatch:

concurrency:
  group: ci-schedule-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test Schedule Worker
    runs-on: self-hosted
    container: python:3.13-alpine
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      
      - name: Cache pip dependencies
        uses: actions/cache@v6
        with:
          path: /root/.cache/pip
          key: ${{ runner.os }}-pip-schedule-worker-${{ hashFiles('schedule/handlers/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-schedule-worker-
      
      - name: Install system dependencies
        run: |
          apk update
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio Expression aiofiles
      
      - name: Run tests with pytest
        run: pytest tests/ -v --tb=short